/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: false, // Disable React's Strict Mode// Add trailing slashes to URLs
	// Other configurations can be added here
}
  
module.exports = nextConfig
  